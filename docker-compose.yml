services:
  bottleimp:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - DB_PATH=/app/data/bottleimp.db
      - BOT_DELAY_MS=800
    volumes:
      - ./data:/app/data
    restart: unless-stopped
